import { StyleSheet } from "react-native";

export const PrimaryListStyles = StyleSheet.create({
  container: {
    display: 'flex',
    alignItems: 'center',
    flexDirection: 'row',
    flexWrap: 'wrap',
  }
});
