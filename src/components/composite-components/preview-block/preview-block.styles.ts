import { StyleSheet } from "react-native";

export const PreviewBlockStyles = StyleSheet.create({
  wrapper: {
    minHeight: 120
  },
  gradient: {
    padding: 12,
    borderRadius: 25,
    alignItems: 'center',
    justifyContent: 'center'
  },
  text: {
    fontSize: 16,
    fontWeight: "600",
    color: '#777171',
    marginBottom: 8
  }
});
